import { useState } from "react";

const Login = () => {
  return (
    <div>
      <h2 className="text-red-500">This is the login page</h2>
      <p>You must provide a valid credential to access this page.</p>
    </div>
  );
};

export default Login;
